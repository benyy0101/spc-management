# SPC 구조 금융상품 회계 시스템

> 금융상품 lifecycle 이벤트를 자동분개 → 원장 → 재무제표까지 연결하는 도메인 중심 회계 엔진

## 한 줄 요약
선박 유동화 펀드·SPC 구조에서 발생하는 거래 이벤트를 회계주체별로 정확한 분개와 원장으로 전환하고, 기준일 재무제표와 투자자 배분까지 산출하는 풀스택 회계 시스템이다.

---

## 프로젝트 배경

선박 유동화 펀드와 SPC 구조에서는 거래·계약·회계 숫자가 강하게 연결된다.
기존에는 수기 처리에 의존하던 이 흐름을 **표준화**하고, 이벤트부터 분개·원장·보고 숫자까지 **추적 가능한 구조**로 만드는 것이 이 프로젝트의 출발점이다.

**핵심 과제:**
- 단순 CRUD가 아닌 **금융상품 엔진 + 회계 엔진** 성격
- 화면보다 먼저 `거래 이벤트 → 분개 → 원장 → 재무제표` 흐름을 정확히 정의해야 함
- 감사 대응이 가능한 이력과 내부통제 구조가 필수

---

## 핵심 회계 흐름

```
┌─────────────┐    ┌──────────────┐    ┌──────────┐    ┌──────────────┐    ┌──────────────┐
│ 이벤트 입력  │───▶│ 도메인 규칙   │───▶│ 자동분개  │───▶│  원장 반영    │───▶│  재무제표    │
│             │    │ (분개 엔진)   │    │ (전표)    │    │ (시산표)     │    │ BS/PL/CF    │
└─────────────┘    └──────────────┘    └──────────┘    └──────────────┘    └──────────────┘
                                                                                  │
                                                                           ┌──────▼──────┐
                                                                           │ 투자자 배분  │
                                                                           │ (pro-rata)  │
                                                                           └─────────────┘
```

운용·회계 담당자가 **회계 이벤트를 입력**하면, 시스템은 도메인 규칙에 따라 **자동분개를 생성**하고, 이를 원장에 반영하여 **기준일 재무제표와 투자자 배분**까지 산출한다.

---

## 아키텍처

### 모노레포 + 클린 아키텍처

```
spc/
  apps/
    api/            ← Fastify 기반 REST API
    web/            ← Next.js 기반 운영 콘솔
  packages/
    domain/         ← 회계 규칙 엔진 (프레임워크 독립)
    application/    ← 유스케이스, 포트, DTO
    db/             ← Drizzle 스키마, 마이그레이션
    test-fixtures/  ← fixture 로더 및 타입
  fixtures/         ← 도메인 정답지 (YAML)
  docs/             ← 기획·도메인·개발 문서
```

### 계층 분리 원칙
- **domain**: 회계 규칙과 분개 생성 로직. DB·HTTP에 의존하지 않는 순수 비즈니스 로직
- **application**: 유스케이스 오케스트레이션. 포트 인터페이스로 인프라와 분리
- **db**: Drizzle 스키마와 어댑터. application의 포트를 구현
- **api/web**: 인프라 계층. domain/application을 소비

이 구조 덕분에 회계 규칙 엔진을 **DB나 HTTP 프레임워크 변경 없이 독립적으로 테스트·교체**할 수 있다.

---

## 기술 스택

| 영역 | 기술 | 선택 이유 |
|------|------|-----------|
| **Monorepo** | pnpm workspace + Turborepo | 패키지 간 의존성 관리와 빌드 파이프라인 최적화 |
| **API** | Fastify + TypeScript | 경량·고성능 서버. MVP 속도와 확장성 균형 |
| **Web** | Next.js + React + TypeScript | SSR/RSC 기반 운영 콘솔. shadcn/ui로 빠른 UI 구축 |
| **DB** | PostgreSQL + Drizzle ORM | 관계·트랜잭션·기준일 집계·감사 추적에 최적. 타입 안전한 쿼리 |
| **테스트** | Node test runner + Fixture | fixture 기반 시나리오 검증으로 회계 정합성 보장 |
| **배포** | Railway (Docker) | 모노레포 서비스 분리 배포 |

---

## 주요 기능

### 1. 자동분개 엔진
- 이벤트 타입별 분개 규칙을 도메인 계층에 정의
- 이벤트 입력 시 규칙에 따라 **전표 헤더 + 차변/대변 분개 라인을 자동 생성**
- 차변/대변 합계 일치 검증, 회계주체(tenant/entity) 범위 강제

### 2. 재무제표 3종
- **재무상태표 (Balance Sheet)**: 기준일 자산/부채/자본 잔액
- **손익계산서 (Profit & Loss)**: 기간 기준 수익/비용 흐름
- **현금흐름표 (Cash Flow)**: 간접법 기준 현금흐름
- 계정과목 ↔ 재무제표 라인 **매핑 관리** 기능 포함

### 3. 회계 기간 마감 (Close Period)
- 기간별 마감 상태 관리: `open → closing → closed → reopened`
- 마감된 기간에 대한 **posting 차단** 로직 내장
- 숫자를 만드는 것에 그치지 않고, **기간을 통제**할 수 있는 운영 기능

### 4. 투자자 배분
- **pro-rata 기준** 배분 실행 및 결과 조회
- 투자자별 지분·약정·납입금·배분 이력 확인
- 향후 waterfall / hurdle / carry 확장 구조 확보

### 5. 운영 통제 및 감사
- **수기분개**: 시스템 외 조정 분개 입력
- **전표 승인**: 내부통제 워크플로우
- **역분개**: 오류 전표 취소 처리
- **이벤트 재처리**: 규칙 변경 시 재생성
- **감사 로그**: 모든 운영 액션의 이력 추적

### 6. 기준정보 관리
- 테넌트 / 회계주체(Entity) / 계정과목(COA) / 금융상품 / 계약 CRUD
- 멀티테넌시: 단일 DB + `tenant_id` 기반 데이터 격리

---

## 지원 이벤트 유형

시스템이 현재 지원하거나 fixture로 관리하는 주요 회계 이벤트:

| 구분 | 이벤트 |
|------|--------|
| **출자/투자** | 투자자 출자, 펀드의 SPC 출자 |
| **자산 운용** | SPC의 자산(대출채권) 취득, 차입 실행 |
| **이자/원금** | 이자 발생(accrual), 이자 수취, 원금 상환 |
| **평가/손상** | 공정가치 평가, 손상 |
| **외화/결산** | 외화 환산, 월말 마감 |
| **배분** | 현금 워터폴, 투자자 배분 |

---

## 설계 의사결정

### Fixture-First 개발 전략
일반적인 seed 데이터가 아닌 **fixture를 도메인 정답지**로 사용한다.
코드 작성 전에 아래 기대값을 먼저 정의하고, 코드가 이를 만족하는지 검증하는 방식이다.

```
fixture 정의 순서:
  1. 입력 이벤트
  2. 기대 분개
  3. 기대 원장 잔액
  4. 기대 재무제표
  5. 기대 투자자 배분
```

이 전략의 장점:
- 회계 규칙의 **정합성을 코드보다 먼저 검증**
- 시나리오 기반 회귀 테스트 자동화
- API, 도메인 규칙, DB seed가 **동일한 시나리오 기준**으로 정렬

### 멀티테넌시 & 데이터 전략
- 단일 DB + `tenant_id`로 시작, 이후 schema/DB 분리로 확장 가능
- 원천 데이터는 **정규화**, 읽기 모델은 필요 시 비정규화
- 회계 시스템의 데이터 중복과 정합성 붕괴가 치명적이므로 **RDB를 기본 선택**

### 도메인 엔진 독립성
- 회계 규칙 엔진(`packages/domain`)은 DB·HTTP와 **완전히 분리**
- 프레임워크 교체 없이 규칙 엔진만 단독 테스트·배포 가능
- application 계층의 포트 인터페이스로 인프라 의존성 역전

---

## 개발 프로세스

이 프로젝트는 아래 순서로 체계적으로 진행되었다:

```mermaid
graph LR
    A[도메인 학습<br/>체크리스트·용어집] --> B[기획서 작성<br/>범위·전제 확정]
    B --> C[Fixture 구축<br/>기대 분개 카탈로그]
    C --> D[스키마·DB 설계<br/>Drizzle + Migration]
    D --> E[Domain/App 계층<br/>회계 규칙 엔진]
    E --> F[API 1차 MVP<br/>핵심 회계 흐름]
    F --> G[Web 1차 MVP<br/>운영 콘솔]
    G --> H[API 2차<br/>재무제표·마감·배분]
    H --> I[Web 2차<br/>재무제표·운영 화면]
```

---

## API 엔드포인트 요약

### 핵심 회계
| Method | Endpoint | 설명 |
|--------|----------|------|
| `POST` | `/accounting-events` | 회계 이벤트 입력 → 자동분개 생성 |
| `GET` | `/events` | 이벤트 목록 조회 |
| `GET` | `/events/:id` | 이벤트 상세 조회 |
| `GET` | `/journals` | 전표 목록 조회 |
| `GET` | `/journals/:id` | 전표 상세 조회 |
| `GET` | `/ledger/trial-balance` | 시산표 조회 |

### 재무제표
| Method | Endpoint | 설명 |
|--------|----------|------|
| `GET` | `/financial-statements/balance-sheet` | 재무상태표 |
| `GET` | `/financial-statements/profit-loss` | 손익계산서 |
| `GET` | `/financial-statements/cash-flow` | 현금흐름표 |
| `GET/POST/PATCH` | `/statement-mappings` | 재무제표 매핑 관리 |

### 운영 통제
| Method | Endpoint | 설명 |
|--------|----------|------|
| `POST` | `/journals/manual` | 수기분개 |
| `POST` | `/journals/:id/approve` | 전표 승인 |
| `POST` | `/journals/:id/reverse` | 역분개 |
| `POST` | `/events/:id/reprocess` | 이벤트 재처리 |
| `GET/POST/PATCH` | `/close-periods` | 마감 기간 관리 |
| `POST` | `/allocations/run` | 투자자 배분 실행 |
| `GET` | `/audit-logs` | 감사 로그 조회 |

---

## 도메인 기준

| 항목 | 기준 |
|------|------|
| 회계 기준 | 일반기업회계기준 |
| 기준 통화 | USD |
| 보고 통화 | KRW 병행 확장 고려 |
| 우선 자산군 | `loan_receivable` (대출채권) |
| 현금흐름표 | 간접법 |
| 투자자 배분 | pro-rata (waterfall 확장 예정) |
| 멀티테넌시 | 단일 DB + `tenant_id` |

---

## 로드맵

- [x] **도메인 설계**: 체크리스트·용어집·기대 분개 카탈로그
- [x] **데이터 기반**: Drizzle 스키마·마이그레이션·fixture 검증
- [x] **회계 엔진**: domain/application 계층, 분개 규칙
- [x] **1차 MVP**: 이벤트 입력 → 자동분개 → 시산표 (API + Web)
- [x] **2차 MVP**: 재무제표·마감·배분·운영통제·감사 (API + Web)
- [ ] **확장**: 외화 환산·공정가치 평가·손상·워터폴·고도화된 승인 체계
